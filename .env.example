# All keys are optional. The demo runs fully on checked-in fixtures,
# model artifacts, the seeded database, and cached notice outputs.

# Live LLM notice generation (optional)
OPENAI_API_KEY=

# Live Plaid sandbox ingest, stretch milestone M6 (optional)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
